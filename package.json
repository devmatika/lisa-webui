{
  "name": "hermes-webui-devtools",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling + white-label branding helper for hermes-webui. The app remains pure Python + vanilla JS with no bundler. `npm run build` validates branding config for customer deployments.",
  "scripts": {
    "lint": "npm run lint:runtime",
    "lint:runtime": "eslint --no-config-lookup -c eslint.runtime-guard.config.mjs \"static/**/*.js\"",
    "build": "node scripts/apply-branding.mjs"
  },
  "devDependencies": {
    "eslint": "^10.4.0"
  }
}
